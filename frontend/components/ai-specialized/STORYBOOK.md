# AI Specialized — Storybook

Stories colocated: `*.stories.tsx` cạnh từng widget trong `components/ai-specialized/`.

```bash
cd frontend
pnpm run storybook          # http://localhost:6006
pnpm run build-storybook    # thư mục storybook-static/
```
