"use client"

/**
 * NOTE: This component is no longer used. Variations now live INSIDE `CreativeSection`
 * (between Primary Text/Headline above and Description/CTA below), so there is no need
 * for a separate per-variant card wrapper.
 *
 * Kept as an empty re-export to avoid breaking any stale imports. Safe to delete once
 * confirmed no external consumers reference it.
 */
export const AdVariantCard = null
