# Ảnh Help & Docs

Đặt file ảnh tại đây để hiển thị trong trang `/help`. Tên file khớp với tham chiếu trong `content/help/*.md` (ví dụ `slack-incoming-webhooks-enable.png`).

Xem danh mục gợi ý: `docs/alert-center/images/README.md` trong repository.
